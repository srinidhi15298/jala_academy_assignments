# 1. Write a function to add integer values of an array
#no arrays in python we can use lists
list =[1,2,3,4]
sum=0
for i in range(0,len(list)):
    sum=sum+list[i]
print("added values of list is ",sum)